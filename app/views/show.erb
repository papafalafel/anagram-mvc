<h1>Anagrams for <%= "#{@word}" %></h1>
<center><img src="/images/assets/arrow.png" alt="arrow" style="width: 50px; height: 50px; text-align: center;" /></center>
<ul class="list">
  <% if !@anagrams.nil? %>
  <% @anagrams.each do  |word| %>
  <li>
    <%= word %>
  </li>
  <% end %>
  <%else %>
  <%= "No Anagrams for this word" %>
  <% end %>
</ul>
